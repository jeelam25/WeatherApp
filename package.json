{
  "name": "expressweb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix src && npm run build --prefix src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "chandra sekhar web",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2", 
    "hbs": "^4.2.0",
    "nodemon": "^3.0.2"
  }
}
